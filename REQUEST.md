# 依頼: 品質改善と再生制御機能の追加

## 大前提(必ず守ること)

このプロジェクトは**再利用可能なライブラリ**です。`examples/basic-player` は
その使い方を示すデモにすぎません。

- 今回追加する機能は、すべて**ライブラリ本体(src/)の公開APIとして実装**すること
- `examples/basic-player` は、その公開APIを呼び出すだけの薄い利用側に留めること
- デモアプリ側にしか存在しないロジック(状態管理、速度計算、ボタン制御など)を
  作らないこと。他のアプリから使ったときに同じことができなくなる
- 公開APIの変更は `src/index.ts` の型定義と README に反映すること

---

# Part 1: 現状の品質問題への対応

外部レビューで以下の指摘を受けました。**まず実コードで裏を取り**、事実かどうかを
報告してください。事実であれば修正、事実でなければその旨を根拠付きで説明して
ください。

## 1-1. Worker要求の直列化(要検証・優先度高)

指摘内容:
- `video-decode-worker.ts` が受信メッセージをそのまま非同期実行しており、
  要求のキューイングがされていない
- `video-decode-core.ts` に `busy` フラグはあるが、要求の待ち行列化には
  使われていない
- そのため以下の操作が短時間に連続すると競合しうる
  - 再生中のseek / seek直後の別seek / 読み込み中の別ファイル読み込み
  - デコーダー破棄直後の再初期化 / 古いdecodeの完了前に新しいdecode
- 古いデコーダーの解放完了を待たずに新デコーダーを初期化する経路がある

依頼:
- 上記が実際に起こりうるか、コード上の具体的な経路を示して検証すること
- 起こりうる場合、Worker内に要求キューを設け、以下の順序を保証すること
  1. decode完了 → 2. invalidate完了 → 3. 旧デコーダー解放完了 → 4. 新デコーダー初期化
- 競合を再現するテスト(連続seek、seek中のファイル差し替え等)を追加すること

## 1-2. フレームバッファの再利用(優先度高)

診断値 `pooledVideoFrames: 0` の通り、CPU側のフレームメモリ再利用が未実装です。
`extractPlanes()` がフレームごとにY/U/V配列を生成し、`clearFrames()` /
`evictFramesBefore()` は参照を外すだけになっています。

依頼:
- フレームバッファを固定長リングバッファ化するか、プールから再利用する形に変更
- 目標は、定常再生中に新規TypedArray割り当てが発生しない状態
- 併せて、**WebGL2 が使える場合は `UNPACK_ROW_LENGTH` を使い、stride付きのまま
  転送して `extractPlanes` のコピー自体を省けないか**検討すること
  (調査報告で挙がっていた案。現在のレンダラはWebGL1)

## 1-3. バッファ上限をメモリ量基準にする(優先度中)

現在キュー上限は「秒数」でしか管理されておらず、9秒で1.2〜1.5GBに達します。

依頼:
- 上限を「秒数」ではなく「バイト数(またはフレーム数)」で管理できるようにし、
  秒数と併用して先に到達した方で頭打ちにすること
- 上限値は**ライブラリの設定オプションとして外部から指定可能**にすること
  (ホストアプリの想定環境に応じて調整できるように)
- デフォルト値は、8GB程度のPCでも他アプリと共存できる水準を根拠付きで設定すること

## 1-4. 軽微な修正(優先度低)

- `package-lock.json` のバージョンが `0.1.0` のまま。`package.json` に合わせること
- README が現状と乖離している(「全映像をメモリ保持」「ストリーミングは後続段階」
  「既定値はlegacy」等)。現在の実装に合わせて全面的に見直すこと
- 非表示タブで終端まで再生した際、`playing` → `ended` の状態遷移がタブ再表示まで
  遅れる可能性がある。状態通知だけはrAFに依存せず行えるようにすること

---

# Part 2: 再生制御機能の追加

## 2-1. 追加する機能

以下をすべて**ライブラリの公開APIとして**実装してください。

| 機能 | 内容 |
|---|---|
| 早送り | 1.5倍速、2倍速、4倍速 |
| 早戻し | 1.5倍速、2倍速、4倍速 |
| コマ送り | 1フレーム進む / 1フレーム戻る |
| スキップ | 5秒進む / 5秒戻る |
| シーク中UI | シーク中は「読み込み中」状態を通知し、操作を非活性にできるようにする |
| 描画デフォルト | GPU描画(yuv-webgl)をデフォルトにする |

## 2-2. 重要な制約(必ず設計に織り込むこと)

**現状のデコードスループットは約1.10倍リアルタイムです。**

- 2倍速には約2.2倍、4倍速には約4.4倍のスループットが必要で、
  全フレームをデコードする方式では**物理的に不可能**です
- 早戻しはMPEG-2 Long-GOPの性質上さらに高コストです
  (後ろ向きにデコードできないため、GOPごとにIフレームから前向きにデコードして
   逆順に出す必要がある)

したがって以下の方針で設計してください。

- **低速域(〜1.5倍程度)**: 全フレームデコード
- **高速域(2倍・4倍、および早戻し全般)**: Iフレーム(キーフレーム)のみを
  デコードして間引き表示する方式に自動的に切り替える
  - MXF index table から Random Access Point は既に取得できているはず
    (`essenceDecodeStart` / `findSeekPoint`)
- **切り替え閾値は固定値でハードコードせず、設定可能にすること**。
  将来スループットが改善した際に、より高速域まで全フレームデコードできるように
  するため
- 実際にどのモードで動作しているかは診断情報として取得できるようにすること

**この方針で問題がないか、実装前に一度確認を取ってください。** 別案(例: 早戻しは
全速度でIフレームのみ、など)がより適切と判断する場合は提案してください。

## 2-3. API設計の要件

- 再生速度は `setPlaybackRate(rate)` のような形で設定でき、負値で逆再生を
  表すか、方向を別に持つかは設計判断に任せます(理由を説明すること)
- コマ送り・スキップは `stepFrame(n)` / `seekRelative(seconds)` のような
  相対移動APIとして提供すること
- **シーク中/バッファリング中の状態は、ホストアプリがUIを制御できるよう
  コールバックまたは購読可能な状態として公開すること**。
  「ボタンを非活性にする」判断自体はホストアプリ側の責務なので、ライブラリは
  そのための状態を提供するに留めること
- 音声の扱いを明記すること(高速再生時はミュートするのか、1.5倍まではピッチ補正
  して鳴らすのか等)。設計判断とその理由を報告に含めること
- GPU描画をデフォルトにする際、WebGLが使えない環境ではCPU描画に自動フォール
  バックすること。フォールバックしたことは診断情報で分かるようにすること

---

# Part 3: 進め方

- **1つの巨大なPRにしないこと。** 以下の単位に分けて、それぞれPRを作成してください
  1. Part 1-1(Worker直列化) ※検証の結果、修正不要なら報告のみ
  2. Part 1-2(フレームバッファ再利用)
  3. Part 1-3(メモリ基準のバッファ上限)
  4. Part 1-4(package-lock / README / 状態通知)
  5. Part 2(再生制御機能の追加)
- 各PRで `npm test` / `typecheck` / `lint` / `build` / `build:example` を通すこと
- `AGENTS.md` のルール通り、main への直接pushは行わず、必ずブランチ+PRで進めること
- `package.json` のバージョンを適切に更新すること

## 検証について

自動テストの成功は「コードの整合性」を示すだけで、実MXFの再生性能を保証しません。
可能な範囲で、**表示状態のタブで**実ファイルを再生し、以下を確認して報告して
ください。

- 再生開始から終端までの映像・音声同期
- 各速度での動作(1.5x / 2x / 4x、順方向・逆方向)
- コマ送り・5秒スキップの正確性(狙ったフレームに移動できているか)
- 再生中のメモリ推移
- 自動確認できない項目は「未検証」と明記すること

## 報告してほしいこと

- Part 1 の各指摘が事実だったか(根拠となるコード箇所付き)
- Part 2 の設計判断とその理由(特に高速再生の方式、音声の扱い)
- 実測できたスループットとメモリ使用量
- 未解決のまま残っている制約
