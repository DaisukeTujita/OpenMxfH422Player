# 依頼: 再生UIの拡張機能

これは REQUEST.md の Part 2(再生制御機能)に続く追加依頼です。まず Part 2 の
PRがどこまで進んでいるか確認し、その状態を前提に作業してください。

## 大前提(REQUEST.mdと同じ)

このプロジェクトはライブラリです。`examples/basic-player` は薄いデモに留め、
今回の機能もすべて**ライブラリ本体(src/)の公開APIとして実装**してください。
計測・状態管理はライブラリ、見た目(スピナーの絵、メーターの描画、配置、
オン/オフのUI)はデモ側、という役割分担を徹底してください。

---

## 1. 現在の再生速度が分かるようにする

- 現在の再生速度を取得できるAPI(例: `playbackRate` プロパティ、または
  `getPlaybackRate()`)を提供すること
- 速度が変化した際に通知するコールバック/イベント(例: `onPlaybackRateChange`)
  を提供すること。再生開始・速度変更ボタン操作・自動フォールバックなど、
  どの経路で変わっても通知されること
- デモ側は、この状態を使って選択中の速度ボタンをハイライト表示すること

## 2. 再生中に速度を変更できるようにする

- Part 2 で実装した速度変更APIについて、**一時停止しないと呼べない/
  内部的に一度停止してから再開する実装になっていないか**を確認すること
- 再生を止めずにシームレスに速度を切り替えられるようにすること
- 特に「全フレームデコード」⇔「Iフレーム間引き」のモード切り替えを伴う速度変更
  (Part 2で設計した閾値をまたぐ場合)で、映像が固まる・音声が途切れる・
  同期がずれるなどの不自然な挙動が出ないか実機で確認し、報告すること

## 3. 待ち状態のローディング表示(スピナー)

Part 2 で依頼した「シーク中はローディング中表示にしてボタンを非活性にする」を、
seek以外の待ち状態も含めて一般化してください。

- ライブラリは、購読可能な形で現在の状態を公開すること。例:
  `'idle' | 'loading' | 'seeking' | 'buffering' | 'playing' | 'paused' | 'ended' | 'error'`
  のような列挙値と、それが変化した際のコールバック
- 対象となる「待ち状態」は最低限これらを含むこと
  - ファイル読み込み中
  - シーク中
  - 速度変更で再初期化が発生している間
  - バッファ枯渇による再生停止(buffering)中
- デモ側は、これらの状態の間はスピナーを表示し、関連する操作ボタン
  (再生/一時停止/シーク/速度変更など)を非活性にすること

## 4. 音声ゲージメータ(オーディオレベルメーター)

### ライブラリ側(計測)

- 再生中の音声レベルを一定間隔で取得できるAPIを提供すること
  (例: `onAudioLevelUpdate(callback)` で定期通知、または
  都度問い合わせ可能な `getAudioLevels()`)
- 値はチャンネルごとのピーク値、または PPM/VU相当のレベル値とすること
  (単位・レンジは設計判断で構わないが、README/型定義に明記すること)
- **計測対象は先頭の1ch・2chのみ**。3ch以上ある音声ファイルでも、
  3ch目以降は計測しなくてよい
- 計測が不要な場合に負荷をかけないよう、有効/無効を切り替えられる
  オプションを用意すること(例: `enableAudioLevels: boolean`)。
  無効時は計測処理自体を止め、CPU負荷が増えないようにすること

### デモ側(表示)

- 動画の右端に、1ch・2chのレベルメーター(縦バー等)を表示すること
- オン/オフを切り替えられるUI(トグルボタン等)を用意すること。
  トグルはライブラリの `enableAudioLevels` と連動させ、非表示時は
  計測自体も止めること
- メーターの見た目(色、目盛り、更新頻度の間引きなど)はデモ側で
  自由に設計してよい

---

## 進め方

- Part 2 のPRが未完了・未マージの場合は、その状況を報告した上で、
  完了後にこの依頼に着手すること
- 引き続き `AGENTS.md` のルール通り、ブランチ+PRで進めること
- `npm test` / `typecheck` / `lint` / `build` / `build:example` を通すこと
- 型定義(`src/index.ts`)とREADMEに、追加した公開APIを反映すること

## 報告してほしいこと

- 上記1〜4それぞれの実装内容とAPI設計
- 2で確認した「速度変更のシームレス性」の実機検証結果
- 4のレベル値の単位・更新頻度と、その設計理由
- 未解決の制約があれば明記すること
