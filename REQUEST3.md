# 依頼: 音声レベルメーターの修正とUI変更

## 前提(これまでと同じ)

ライブラリ(src/)は計測・状態、デモ(examples/basic-player)は見た目、という
役割分担を維持してください。

---

## 1. 音声メーターがまったく動かない原因の調査・修正

現象: トグルをオンにしても、再生中に音声メーターの表示がまったく動かない
(値が変化しない/常に無音のまま)。

以下の観点で原因を切り分けてください。

- デモのトグルUIが、`enableAudioLevels` の切り替え(`ref.setAudioLevelsEnabled(true)`
  など)を実際に呼び出せているか。クリックしても内部状態が変わっていない
  だけの可能性がある
- `onAudioLevelUpdate` のコールバックが実際に発火しているか(デモに一時的な
  console.logを入れて確認するなどでよい)
- 前回の実装報告にあった以下の既知の制約に該当していないか
  - muted で初期化していると常に無音を返す仕様になっている
  - legacyモードでは意味のある値が取れない設計になっている
  - 再生中でない場合は静音を返す仕様になっている
- 読み込んでいるテスト用MXFに、そもそも音声エッセンスが含まれているか、
  現在のモード(legacy/streaming)の組み合わせで音声デコード自体が
  行われているか
- 上記のいずれにも該当しない場合、`getAudioLevels()` / `onAudioLevelUpdate`
  の計測ロジック自体(audio-levels.ts等)にバグがないか確認すること

原因を特定し、実装またはデモ側の配線を修正してください。「本来無音になる
条件で無音になっているだけ」だった場合は、その旨を報告し、デモ側で
分かりやすいテスト手順(ミュート解除、streamingモード、1.0倍速で確認、等)
を用意してください。

## 2. 表示位置とサイズの変更

- 現在、音声メーターは動画の外に配置されているとみられるので、
  **動画の上にオーバーレイ表示**する形に変更してください(動画の右端に
  重ねて表示)
- メーターの**高さは、動画表示エリアの高さの半分程度**にしてください
  (上下中央揃えを想定していますが、見た目のバランスで調整して構いません)
- オーバーレイなので、動画の操作(再生/一時停止等のクリック領域)を
  邪魔しない位置・大きさにすること
- 既存のオン/オフトグルの挙動はそのまま維持すること

---

## 進め方

- 引き続き `AGENTS.md` のルール通り、ブランチを切ってPRを作成すること
  (ローカルでの作業でも、mainへの直接pushはしないこと)
- `npm test` / `typecheck` / `lint` / `build` / `build:example` を通すこと
- 1の原因(根拠となるコード箇所)と、2の変更内容を報告してください