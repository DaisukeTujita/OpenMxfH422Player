# 依頼: 低メモリ環境でのバッファ適応化と効果測定手段の追加

## 背景

5年前購入・メモリ8GBのWindows 11機(Surface Pro 7)で再生すると、頻繁に
停止する。実機で確認したところ:

- ブラウザ起動直後の時点でメモリ使用率80%以上
- 動画選択後は90%程度まで上昇
- 固まっている間、デモの診断表示は一貫して「buffering」を示している

現状の設計(9秒バッファ・約1.2〜1.5GBのJSヒープ)は、このマシンには
そもそも収まらない量を確保しようとしていると考えられる。メモリ不足で
OS側のページングが起きると、デコードのスループットがさらに落ち、
バッファリングが悪化する悪循環が起きている可能性が高い。

---

## 大前提(これまでと同じ)

ライブラリ(src/)に判断・制御ロジックを実装し、デモ(examples/basic-player)
はその状態を表示するだけに留めること。バッファサイズの目標値や判定閾値は、
ホストアプリ側からオプションで上書きできるようにすること。

---

## 1. バッファ目標をメモリの実際の余裕に応じて動的に調整する

### 推奨する設計の方向性(最終判断はお任せします)

`navigator.deviceMemory` は仕様上8以上がすべて「8」に丸められるため、
8GBの当該機と、それより大きいメモリを積んだ機種を区別できません。
また今回のように「動画を選ぶ前からブラウザ自体がメモリを80%使っている」
状況は、起動時の静的な機種判定では捉えられません。

そのため、**起動時の1回きりの判定ではなく、再生中も定期的に実際の
メモリ余裕を見て、バッファ目標を動的に増減させる**方式を推奨します。

- Chrome/Edge系ブラウザで利用できる `performance.memory`
  (`jsHeapSizeLimit` / `usedJSHeapSize`)などから、現在のヒープ使用状況・
  余裕を定期的に取得する
- 余裕が少ない場合は、バッファ目標(秒数・バイト数)を下げ、既存の
  eviction(破棄)処理をより積極的に働かせる
- 余裕がある場合は、通常の目標値まで許容する
- `performance.memory` が使えない環境(Firefox等)のフォールバックとして、
  保守的なデフォルト目標値と、ホストアプリが明示的に指定できる
  設定オプション(バイト数または秒数の上限)を用意する

この方式が難しい・別の設計の方が良いと判断した場合は、その理由とともに
代案を提示してください。

### 満たしてほしい要件

- バッファ目標のデフォルト値・調整ロジックの閾値は、すべて設定オプション
  として外部から上書き可能にすること(将来のチューニングのため)
- 目標を下げた結果、極端に短いバッファでカクつきが悪化しないよう、
  最低限確保する下限値も設けること
- 挙動の変化(目標を上げた/下げた)を診断情報として取得できるようにすること

---

## 2. 効果を判断できる測定手段の追加

実機でしか正確な検証ができないため、**変更前後を数値で比較できる仕組み**
を用意してください。

### 追加してほしい診断情報(既存の「Streaming診断」パネルに追加)

- 再生開始からの**バッファリング発生回数**
- 再生開始からの**バッファリング合計時間**(ms)
- 現在のバッファ目標値(秒数・バイト数)と、実際に保持しているバッファ量
- 取得できる場合は、現在のJSヒープ使用量・余裕(`performance.memory`由来)
- バッファ目標が動的に増減した回数・タイミング(簡易なログでよい)

### 検証方法の提案

- 上記の値をコンソールにも出力するか、診断パネルからコピーできる形に
  すること(スクリーンショットだけでなく数値を残せるように)
- 可能であれば、同一ファイルを一定時間(例: 3分)再生した際の
  「バッファリング回数」「バッファリング合計時間」をひとまとめに
  表示する簡易サマリーを用意すること。変更前後で同じ条件で再生し、
  この数値を比較することで効果を判断する想定

---

## 進め方

- 引き続きブランチ+PRで進めること
- `npm test` / `typecheck` / `lint` / `build` / `build:example` を通すこと
- 1の設計判断(採用した方式とその理由)、2で追加した診断項目の一覧を
  報告してください
- 開発環境(実機ではない)での動作確認はもちろん、可能な範囲で
  「意図的にメモリを圧迫した状態」を再現するテスト(例: 大きな配列を
  確保してから再生する等)があれば、その結果も報告してください
