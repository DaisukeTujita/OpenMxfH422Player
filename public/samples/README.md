# Local MXF samples

Place licensed local test material in this directory, for example `example.mxf`.
The ignore rules keep MXF binaries out of Git; Vite serves the example as
`/samples/example.mxf`.
